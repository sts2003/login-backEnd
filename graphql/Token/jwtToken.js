// import jwt from "jsonwebtoken";
// require("dotenv").config();

// export const jwtsign = (userId, userPassword) => {
//   const token = jwt.sign({ userId, userPassword }, process.env.PRIVATE_KEY, {
//     expiresIn: "5h",
//   });
//   return token;
// };
